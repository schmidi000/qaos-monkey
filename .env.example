# QAosMonkey credential examples.
# Copy to .env for local runs, or set the same variables in CI/CD secrets.
#
# Never commit real credentials. `.env` is ignored by git.

QAOSMONKEY_ADMIN_EMAIL=admin@example.test
QAOSMONKEY_ADMIN_PASSWORD=replace-me

